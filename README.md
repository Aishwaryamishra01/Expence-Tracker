# Expence-Tracker


Layout of the web page :- This web page is a simple Expence tracker or Tranjaction Trackerwhich have a two main headings reflecting as Income (which is for earnnings) another is for Expances ( whichis for Expences) and it has a list of all the eaarnings and Expances which is updated using java script as per tee users input in  the form which contains two requird information about the transaction whether its a income or an expances (for expences it requires the negetive sign by the user)............                                                                                                     
HTML file :- 
            the html file contains a div whose class is container which will seperate the project from the body of the web page and will hold all the other contents/ elements of the of the project / Expence tracker web page......                                                                                                                                          
            inside the container div the div with the class summary will hold the Expanses div and the income div . again these two div's are seperate from the trasaction section.                                                                
            the transactioon section :- it is actually a unordered list which is manupulated by the java script as per the user  input.                                                                                                                     
            The form section :- its a form which takes two required imputs form the user (amount and the ammout with the sign if it is a expence).and conatins a button

CSS Styling:-
            This will contain the colors and what they are used for and the explanation of thee styling of the elements is written as coments iside the CSS file itself as comments.                                                                  
            some impotant used properties  and pseduo classes:-                                                                      
                        ::-webkit-scrollbar is not a property but a CSS pseudo-element. It’s part of a set of special selectors that allow you to style the scrollbar in browsers built on WebKit (like Chrome, Safari, and newer versions of Edge). Targets the entire scrollbar area. You can change its width or height.                                                                                                                                                                                   
                        ::-webkit-scrollbar-thumb → Styles the draggable part of the scrollbar (the “handle” you move).                                                                                                                                                                       
                        ::-webkit-scrollbar-track → Styles the track (the background area the thumb slides over).                                                                                                                          
                        ::-webkit-scrollbar-button → Styles the arrow buttons at the ends of the scrollbar (though many browsers hide these by default).                                                                                                                                                                                                                                
                        ::-webkit-scrollbar-corner → Styles the corner where horizontal and vertical scrollbars meet.
            Explanation about thumb and track:-                                                                           Imagine a train track:-   
                                      The track is the rail (fixed background)
                                      The thumb is the train (the moving part you drag).




  java script :- using thee basic java script and updating the list of expenses annd income explicitly and adding the classes in those list items to update the color of the border (red for expenses and green for income ) as these classes are already styled in the css code                                     


  this is all the information i find useful for creating this "Expense Tracker " , and some consept that  came in my knowledg4e while working on it.
  
